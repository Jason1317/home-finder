// HomePriceAgent.js
// Modular agent for fetching home price data
export class HomePriceAgent {
  async fetch(city) {
    // Fetch home price data for city
    return { type: "homePrice", data: null };
  }
}
